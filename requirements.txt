# Streamlit chat UI
streamlit==1.39.0

# Vector store: Qdrant hybrid (HNSW dense + BM25 sparse)
qdrant-client==1.13.3
fastembed==0.6.1

# Dense embeddings (free, open-source)
sentence-transformers==3.2.1

# Document loaders for upload (md / txt / pdf / docx)
PyYAML==6.0.2
pypdf==5.1.0
python-docx==1.1.2

# Auth (JWT, bcrypt-style hashing uses stdlib hashlib so no extra dep)
PyJWT==2.9.0

# Env
python-dotenv==1.0.1

# Optional: only needed if LLM_PROVIDER=gemini
google-generativeai==0.8.3
